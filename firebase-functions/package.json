{
  "name": "coffeecraze-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "@google/genai": "^1.29.0",
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2-rc.0"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  }
}
